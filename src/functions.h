#pragma once

#include <memory>
#include <vector>

uint64_t sumAllCombination(const std::vector<uint64_t> &integers);
bool isPrime(uint64_t n);
uint64_t gcd(uint64_t a, uint64_t b);
uint64_t absoluteDiff(uint64_t a, uint64_t b);
